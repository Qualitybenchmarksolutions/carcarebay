import 'dotenv/config';
import express from 'express';
import cors from 'cors';
import jwt from 'jsonwebtoken';
import bcrypt from 'bcryptjs';
import { Pool } from 'pg';
import Razorpay from 'razorpay';
import fs from 'fs';
import path from 'path';
import multer from 'multer';

const app=express();
const PORT=process.env.PORT||4000;
const pool=new Pool({connectionString:process.env.DATABASE_URL});
const origins=(process.env.CORS_ORIGINS||'*').split(',');
app.use(cors({origin:(o,cb)=>cb(null,!o||origins.includes('*')||origins.includes(o))}));
app.use(express.json({limit:'2mb'}));
const uploadDir=path.resolve(process.env.STORAGE_DIR||'./uploads'); fs.mkdirSync(uploadDir,{recursive:true});
const upload=multer({dest:uploadDir});
const razorpay=process.env.RAZORPAY_KEY_ID&&process.env.RAZORPAY_KEY_SECRET?new Razorpay({key_id:process.env.RAZORPAY_KEY_ID,key_secret:process.env.RAZORPAY_KEY_SECRET}):null;
const q=(text,params=[])=>pool.query(text,params);
const sign=u=>jwt.sign({sub:u.id,role:u.role,name:u.name},process.env.JWT_SECRET,{expiresIn:'7d'});
function auth(req,res,next){try{const h=req.headers.authorization||'';req.user=jwt.verify(h.replace('Bearer ',''),process.env.JWT_SECRET);next()}catch{return res.status(401).json({error:'Unauthorized'})}}
const roles=(...rs)=>(req,res,next)=>rs.includes(req.user.role)?next():res.status(403).json({error:'Forbidden'});
app.get('/health',async(_,res)=>{try{await q('SELECT 1');res.json({ok:true,service:'CarCareBay API',database:'postgresql'})}catch(e){res.status(503).json({ok:false,error:'Database unavailable'})}});
app.post('/api/auth/register',async(req,res)=>{const {phone,name,password,role='customer'}=req.body;if(!phone||!name||!password)return res.status(400).json({error:'phone, name and password required'});const hash=await bcrypt.hash(password,10);try{const {rows}=await q('INSERT INTO users(phone,name,password_hash,role) VALUES($1,$2,$3,$4) RETURNING id,phone,name,role',[phone,name,hash,role]);res.status(201).json({user:rows[0],token:sign(rows[0])})}catch(e){res.status(409).json({error:'Phone already registered'})}});
app.post('/api/auth/login',async(req,res)=>{const {phone,password}=req.body;const {rows}=await q('SELECT * FROM users WHERE phone=$1 AND active=true',[phone]);if(!rows[0]||!await bcrypt.compare(password,rows[0].password_hash||''))return res.status(401).json({error:'Invalid credentials'});const u=rows[0];res.json({user:{id:u.id,phone:u.phone,name:u.name,role:u.role},token:sign(u)})});
app.get('/api/me',auth,async(req,res)=>{const {rows}=await q('SELECT id,phone,name,role,active FROM users WHERE id=$1',[req.user.sub]);res.json(rows[0])});
app.get('/api/plans',async(_,res)=>res.json((await q('SELECT id,name,price_paise,included_exterior,included_interior FROM plans WHERE active=true ORDER BY price_paise')).rows));
app.get('/api/customers',auth,roles('admin'),async(_,res)=>res.json((await q(`SELECT u.id,u.name,u.phone,COUNT(v.id)::int vehicles,COALESCE(MAX(s.status),'none') subscription_status FROM users u LEFT JOIN vehicles v ON v.customer_id=u.id LEFT JOIN subscriptions s ON s.customer_id=u.id WHERE u.role='customer' GROUP BY u.id ORDER BY u.created_at DESC`)).rows));
app.get('/api/apartments',auth,roles('admin'),async(_,res)=>res.json((await q(`SELECT a.*,COUNT(DISTINCT v.id)::int cars,COUNT(DISTINCT s.customer_id)::int subscribers,COALESCE(SUM(p.price_paise),0)::int mrr FROM apartments a LEFT JOIN parking_bays b ON b.apartment_id=a.id LEFT JOIN vehicles v ON v.bay_id=b.id LEFT JOIN users u ON u.id=v.customer_id LEFT JOIN subscriptions s ON s.customer_id=u.id AND s.status='active' LEFT JOIN plans p ON p.id=s.plan_id GROUP BY a.id ORDER BY a.name`)).rows));
app.get('/api/partners',auth,roles('admin'),async(_,res)=>res.json((await q(`SELECT u.id,u.name,u.phone,COUNT(b.id) FILTER(WHERE b.scheduled_at::date=CURRENT_DATE)::int jobs_today,COUNT(b.id) FILTER(WHERE b.scheduled_at::date=CURRENT_DATE AND b.status='completed')::int completed_today,COUNT(b.id) FILTER(WHERE b.scheduled_at::date=CURRENT_DATE AND b.status IN('assigned','in_progress'))::int active_jobs FROM users u LEFT JOIN bookings b ON b.partner_id=u.id WHERE u.role='partner' GROUP BY u.id ORDER BY u.name`)).rows));
app.get('/api/bookings',auth,async(req,res)=>{let sql=`SELECT b.id,b.service,b.scheduled_at,b.status,b.before_photo_url,b.after_photo_url,c.name customer,c.phone, v.make_model car,v.plate, a.name apartment,p.tower,p.floor,p.bay_number bay,pu.name partner FROM bookings b JOIN users c ON c.id=b.customer_id JOIN vehicles v ON v.id=b.vehicle_id JOIN apartments a ON a.id=b.apartment_id LEFT JOIN parking_bays p ON p.id=b.bay_id LEFT JOIN users pu ON pu.id=b.partner_id`;const args=[];if(req.user.role==='customer'){sql+=' WHERE b.customer_id=$1';args.push(req.user.sub)}else if(req.user.role==='partner'){sql+=' WHERE b.partner_id=$1';args.push(req.user.sub)}sql+=' ORDER BY b.scheduled_at DESC';res.json((await q(sql,args)).rows)});
app.post('/api/bookings',auth,roles('customer','admin'),async(req,res)=>{const {vehicle_id,service,scheduled_at,apartment_id,bay_id}=req.body;if(!vehicle_id||!service||!scheduled_at||!apartment_id)return res.status(400).json({error:'vehicle_id, service, scheduled_at and apartment_id required'});const customerId=req.user.role==='admin'?req.body.customer_id:req.user.sub;const {rows}=await q(`INSERT INTO bookings(customer_id,vehicle_id,service,scheduled_at,apartment_id,bay_id) VALUES($1,$2,$3,$4,$5,$6) RETURNING *`,[customerId,vehicle_id,service,scheduled_at,apartment_id,bay_id||null]);res.status(201).json(rows[0])});
app.patch('/api/bookings/:id',auth,async(req,res)=>{const allowed=['status','partner_id','notes','before_photo_url','after_photo_url'];const keys=Object.keys(req.body).filter(k=>allowed.includes(k));if(!keys.length)return res.status(400).json({error:'No editable fields'});const vals=keys.map(k=>req.body[k]);let where='id=$'+(keys.length+1), params=[...vals,req.params.id];if(req.user.role==='customer')where+=' AND customer_id=$'+(keys.length+2),params.push(req.user.sub);if(req.user.role==='partner')where+=' AND partner_id=$'+(keys.length+2),params.push(req.user.sub);const set=keys.map((k,i)=>`${k}=$${i+1}`).join(',');const {rows}=await q(`UPDATE bookings SET ${set}, completed_at=CASE WHEN $1='completed' THEN now() ELSE completed_at END WHERE ${where} RETURNING *`,params);if(!rows[0])return res.status(404).json({error:'Booking not found'});res.json(rows[0])});
app.post('/api/bookings/:id/photos',auth,upload.single('photo'),async(req,res)=>{if(!req.file)return res.status(400).json({error:'photo required'});const type=req.body.type==='after'?'after_photo_url':'before_photo_url';const url=`/uploads/${req.file.filename}`;const {rows}=await q(`UPDATE bookings SET ${type}=$1 WHERE id=$2 RETURNING id,${type}`,[url,req.params.id]);if(!rows[0])return res.status(404).json({error:'Booking not found'});res.json(rows[0])});
app.use('/uploads',express.static(uploadDir));
app.post('/api/ratings',auth,roles('customer'),async(req,res)=>{const {booking_id,score,comment}=req.body;const {rows}=await q('INSERT INTO ratings(booking_id,customer_id,score,comment) VALUES($1,$2,$3,$4) RETURNING *',[booking_id,req.user.sub,score,comment||null]);res.status(201).json(rows[0])});
app.get('/api/dashboard',auth,roles('admin'),async(_,res)=>{const [s,m,j,r]=await Promise.all([q("SELECT COUNT(*)::int n FROM subscriptions WHERE status='active'"),q("SELECT COALESCE(SUM(p.price_paise),0)::int n FROM subscriptions s JOIN plans p ON p.id=s.plan_id WHERE s.status='active'"),q("SELECT COUNT(*)::int n,COUNT(*) FILTER(WHERE status='completed')::int completed FROM bookings WHERE scheduled_at::date=CURRENT_DATE"),q('SELECT COALESCE(ROUND(AVG(score),2),0) n FROM ratings')]);res.json({activeSubscribers:s.rows[0].n,mrr_paise:m.rows[0].n,todaysJobs:j.rows[0].n,completed:j.rows[0].completed,avgRating:Number(r.rows[0].n)})});
app.post('/api/payments/order',auth,roles('customer'),async(req,res)=>{if(!razorpay)return res.status(503).json({error:'Razorpay not configured'});const amount=Number(req.body.amount_paise);if(!amount)return res.status(400).json({error:'amount_paise required'});const order=await razorpay.orders.create({amount,currency:'INR',receipt:`ccb_${Date.now()}`});res.json(order)});
app.post('/api/subscriptions',auth,roles('customer','admin'),async(req,res)=>{const customerId=req.user.role==='admin'?req.body.customer_id:req.user.sub;const {plan_id}=req.body;if(!plan_id)return res.status(400).json({error:'plan_id required'});const {rows}=await q(`INSERT INTO subscriptions(customer_id,plan_id,status) VALUES($1,$2,'active') RETURNING *`,[customerId,plan_id]);res.status(201).json(rows[0])});
app.get('/api/jobs',auth,async(req,res)=>{const {rows}=await q(`SELECT b.id,b.service,b.scheduled_at,b.status,v.plate,v.make_model car,a.name apartment,p.tower,p.floor,p.bay_number bay,u.name customer FROM bookings b JOIN vehicles v ON v.id=b.vehicle_id JOIN users u ON u.id=b.customer_id JOIN apartments a ON a.id=b.apartment_id LEFT JOIN parking_bays p ON p.id=b.bay_id WHERE ($1='admin' OR b.partner_id=$2 OR b.customer_id=$2) ORDER BY b.scheduled_at`,[req.user.role,req.user.sub]);res.json(rows)});
app.listen(PORT,()=>console.log(`CarCareBay API listening on :${PORT}`));
